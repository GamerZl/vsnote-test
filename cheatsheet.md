# Windows
win + cmd - åpne terminal
dir - directory: vise alle filer og kataloger i den katalogen du står i
cd [katalognavn] - change directory: går inn i katalogen
cd.. - gå  en katalog tilbake
mkdir [katalognavn] - make directory: lag en katalog med navnet du oppgir
code . - åpner vsc
code . - åpner vsc (visual studio code) i den katalogen du står i i terminalen
echo > [filnavn]: lager en fil med navnet og skriver noe drit i den som du må slette
demodirectory

Når man prøve fliter en folder. man må skrive: move >

skrive (tree /f) hvis man vil se alle filene



Resbarry when trying to update type: sudo apt update
and then we change it into uppgrade in the next step: for example sudo apt upgrade which upgrades linux.

sudo ufw enable
sudo ufw status
if you get command not found
type sudo apt install ufw to make it work

afterward type
sudo ufw allow ashh
and then write again
sudo ufw status
then type ip a

the last number for the ip address is 19

rm uses we to delete file for example rm hei.txt and when trying to remove a folder which we need to write rm -r shafietestmappe

touch hei.txt is same as ocho > shafietest.txt 
nano uses we when we want to enter a fil for example nano shafie.txt

use nmtui in rasparry if you want to change ip address